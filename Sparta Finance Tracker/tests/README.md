# Tests — Sparta AP Finance Tracker

No build step. Node + the Chromium that ships with Playwright.

```bash
cd "Sparta Finance Tracker/tests"
npm install                 # playwright only
./run-all.sh                # everything
```

| Suite | What it covers | Cases |
|---|---|---|
| `test-data.js` | Pure functions, table-driven: `meCategorise` waterfall, exclusions, dedup fingerprints, `limitFor`/`contributed`, `yfActual`, `parseCSV`, mixed-currency holdings math, the **CAD-only currency invariant**, and the one-time `migrateContribCAD` in all three branches, the split between `meIsPayment` (card payments, kept as Bill Payment rows) and `meIsExcluded` (bank transfers, still dropped), plus the reworked Monthly category list — `Taxi/Rental`, `Transit`, `Indoor`/`Outdoor Entertainment`, `Shopping` and `Fees/Fine` each reached by keyword, and a file column naming a retired category correctly ignored, and the list itself — sorted A→Z with ledger names interleaved rather than appended, Yearly's names kept off it entirely (the two tabs keep separate lists), `Bill Payment` pinned last after the thirteen spending ones, and the row being edited keeping its own name so editing cannot silently blank it | 67 |
| `test-ui.js` | Real interaction flows: tab routing + theme/lock state, dashboard cash & holdings & sell modal & sorting, contributions deposit/log/edit modal, yearly transaction CRUD + categories + filters, monthly shared-ledger + popover, settings drawer + tab reorder, reload persistence, PIN gate Also covers the Dashboard chart's dots and hover readout: every point reports its own figure (checked as seven *distinct* values, so a lookup that always returned the last point could not pass), a past point is labelled with its date and today's with its time, the figure follows the CAD/USD toggle rather than hardcoding a symbol, a range switch clears a tooltip that was open, and a dozen redraws add **zero** further listeners — measured by wrapping `addEventListener`, which reports 36 with the bind guard removed. The opacity reads wait on the settled value rather than sleeping past the 150ms `.ctip` transition; this section really did fail intermittently on `0.969446` first (bug class 11). Also the quote-link arrow beside each ticker: it defaults to that symbol's Yahoo page, carries `rel=noopener` with its `target=_blank`, repoints every row when the base is saved, and resolves identically with or without a trailing slash. `javascript:`, `mailto:` and plain junk are each refused **and leave the previous base standing** — `new URL()` accepts the first two, so the scheme is checked separately. Clearing the field drops the key and falls back to the default, and the base is asserted to be a device preference so Clear data cannot take it. | 94 |
| `test-contrib.js` | Contributor (Abi/Poo) tracking on Contributions: `depositContrib` defaults to Abi, migration on both branches (already-tagged entries untouched, untagged entries backfilled), the yearly-backfill zero-amount guard now split per contributor/account, the old-shape `{tfsa,fhsa}` yearly-draft fallback, stacked-chart segment totals vs. `contributed()`, and the All/Abi/Poo filter — confirms it scopes the chart + deposit log but leaves the TFSA/FHSA hero cards and room-remaining untouched | 28 |
| `test-import.js` | CSV import and the trend filter: refunds/credits arriving as **negative** expenses so they offset their category (they were dropped outright before), card payments kept as **Bill Payment** rows rather than dropped — forced negative, out of the month total, in the ledger — while bank transfers are still excluded outright, `meMoney` signing negatives correctly, re-import **restoring** a row deleted or lost to a bad sync (the remembered-fingerprint list no longer blocks what is absent from the ledger), the month check warning when a statement will be filed under a different month than its dates, auto-detect vs the manual "Expenses appear as" override, and the filter popover being genuinely clickable rather than painted over by the chart. Also the import form's second pass: the dead "Statement source" dropdown and the "Expenses appear as" override both removed (auto-detect reads either convention off the file), a free-text statement source stamped onto every row it imports and rendered as an escaped tag, the month chip on the button row naming what the picker at the top is set to, and the gate that holds "Add to expenses" shut until a bill is chosen — disabled with an amber reminder naming which of the two things is missing, and refused by the handler as well so it is not UI-only | 37 |
| `test-mobile.js` | Phone layout at 390/360/320px: every open tab's controls reachable without side-scrolling (Yearly's Income/Expense filter used to sit 607px past the right edge), the tab bar wrapping so the tab list scrolls on its own row and per-tab controls sit below it, no page-level horizontal pan on any tab, the holdings table scrolling inside its own panel instead of dragging the whole page wide, and Plan rows keeping a usable name width by dropping the amount to a second line. Also covers the fluid wave backdrops on Yearly / Monthly / Archives: each layer is the first child of its field so the motif icons paint over it, stacking is wave 0 / motifs 1, every icon stays at =0.15 opacity so it reads over the brighter ground, each wave animates only on its own tab and is paused elsewhere, and none of them introduce a sideways pan. Also pins Yearly's wave depth: the ribbon reaches ~70% down the viewport, its horizontal extent is unchanged to the pixel (the proof that the stretch is vertical only), the scale is applied after the rotate so the angle holds, and Monthly and Archives keep their untouched transforms Section 9 covers the native date control on Yearly's add-transaction form, which iOS sizes to its own idea of the content: it came out shorter than the Type select beside it and spilled past the card's right edge. Checked on both sides of the 560px breakpoint, since the form swaps font-size *and* padding there. The section carries a written limit — its height and appearance checks were verified to fail against the unfixed file, but the overflow check passes either way, because Chromium's date control has a far smaller min-content width than iOS Safari's and cannot reproduce the bug that prompted it. Section 7 now pins the lengthened ribbon: 81% down rather than 71%, its start unmoved (the stretch is pivoted at the paths' own origin, not a slide — a bare scale shifted the whole thing 51px right), the tail fade living on an inner group so it runs along the ribbon instead of the screen, and **all 64 paths carrying no transform of their own**. That last one guards a real failure: extending each path along its own exit tangent made them stop being parallel offsets and splay into a fan, because every path leaves at a different heading. | 136 |
| `test-plan.js` | Plan tab: segment create/rename/delete (delete confirms first and must not toggle the segment), starting balance, collapse/expand and that clicking the name or balance field does **not** toggle; items sorted by date whatever order they are entered in; running balance and the reported **lowest point** — the numbers most likely to be silently wrong; passed rows greyed with a clock icon yet **still fully editable** (guards "greyed out" from quietly becoming read-only); `normalizePlan()` against junk shapes; `sparta.plan` persistence and `corePayload()` round-trip; `render()` and starting-balance edits leaving focus and caret alone; the blue→`#071520` skin sampled teal→`#071520` translucent skin; and the `.planfield` page backdrop — five drifting wave bands plus five planning motifs behind the shell, the panel staying brighter than the backdrop behind it, and the field fading out AND pausing its animations on every other tab (the guard every backdrop field needs, since `opacity:0` alone does not stop animation); 60/40 split stacking below 900px | 74 |
| `test-notes.js` | Shared notepad on Dashboard + Contributions: confirms the panel is ONE element moved by `applyView()` rather than two copies (the failure this is really guarding against), point add/edit/delete with Enter and Backspace behaviour, prose box, `normalizeNotes()` against junk shapes, `sparta.notes` persistence across a reload, `corePayload()`/`applyPayload()` cloud round-trip, and that `render()` — which fires on every price tick — leaves focus and caret untouched. Also covers the Strategy 60% / **Eye on Stocks** 40% split: chip add via Enter with focus retained so tickers can be typed one after another, case-insensitive de-duping, trimming, casing preserved (not uppercased), Backspace-on-empty removing the last chip, and the split stacking to one column at 900px | 67 |
| `test-yf-me-contrib.js` | Contributor (Abi/Poo) tracking on Yearly Finance + Monthly Expense (they share `state.yf.txns`): `normalizeYF()` migration, `yfSaveTx`/`meSaveTx` add+edit paths, `meApplyImport` defaulting to Abi, and the All/Abi/Poo filter on each tab — confirms Yearly Finance's filter scopes only `#yfTxBody` (category/summary cards untouched) while Monthly Expense's filter scopes the whole month view (total/count/rows), since it has no budget concept to protect |26 |
| `test-bills.js` | The bills & allocations model, the trickiest arithmetic in the app: `spend = Σ{expense && !allot}` over Yearly's own rows, `actual(C) = Σ{cat===C}` with Monthly's rows and every allocation excluded, and the invariant that **Σ actual(C) over every category === spend** — itemising a $1,910 credit-card statement into groceries and gas must move money between categories without changing the year total. Also covers the scoped **"Allot to"** dropdown: it offers only the expenses Yearly Finance actually holds for the month being filed into (July's Vacation + Credit Bill, not all 15 categories), the manual form following its own Month picker while the import follows the tab's month, the empty-month note that names the month, and the `derived:true` migration — a bill can no longer be auto-invented, so a real statement entered later can no longer double-count against a placeholder. An allocation's effect on the breakdown depends on whether it carries a category: **with** one (an imported statement row) it reclassifies — adds to its own category, comes back out of the bill's; **without** one (typed on Add Expense, where filing is either/or) it is only a note and touches neither. The invariant holds under both shapes. Also covers imports keeping their categories alongside the bill, and the one-off recovery of rows a middle build imported with no category — keyed on the import log, so a typed note is never swept up with them. Section 32 pins a $1,850 bill against statements that under- and over-shoot it, and the case where a charge the statement missed is typed in afterwards — with a category it reads −$1 and the sub-line agrees; without one it is a note, the bill keeps its $10 and the sub-line says "$11 noted" instead of crying over. Section 33 pins that a negative category draws an empty bar, not a full-width one. Section 35 covers the case name-based logic breaks on — the SAME category name on both tabs: renaming Yearly's Rent must leave Monthly's alone, a Monthly row must not block deleting the Yearly category it shares a name with, a Monthly expense must never be offered as a bill, and the overage warning must use the same definition of "itemised" as the note beside it. All four were leaks found by auditing, not by a failing test. Section 36 covers the write side: a Yearly bill is read-only on Monthly (editing one there could give it an "Allot to", allotting a bill to another bill and dropping it out of Yearly's total), and deleting a bill detaches what was itemised into it rather than orphaning rows whose money would then sit in no total anywhere. Section 37 closes the last three ways a bill lost track of its itemisation: renaming a bill category leaves its allocations pointing at the same row (`allot` is the bill's `id`, so there is nothing left to repoint), the note follows `allotM` so a December purchase counts toward January's bill across the year boundary, and the import dedup set no longer contains Yearly bills — one could otherwise swallow an identical Monthly purchase as "already imported". Section 38 pins the two notes a bill carries and where each sits: `$X Spend` under the AMOUNT (everything filed against it except the payments, refunds counted in as the negatives they are) and `$Y Bill Paid` under the DESCRIPTION, each printed only when it has rows to report — a payment with no charges shows Bill Paid alone rather than an empty Spend line, an expense with nothing filed carries neither, the EXPENSES table carries no note text at all, and the word "Itemised" appears nowhere. That netted third figure was removed because it only reconciles if you also hold the previous month's bill in your head, and because it bailed out when the net reached zero or less — hiding the note on exactly the bills least accounted for. Sections 39-40 cover the Monthly entry rules: a Monthly category and a bill save together (what the charge was, and what paid for it) while a YEARLY category against a bill is refused and leaves the row untouched, and the top bar graph's labels are upper-cased and carry the same meColor() the trend chart draws each line in. test-mobile's section 8 sweeps all six views at 390 and 320px for reachability — every control on screen, every sideways scroller able to reach its far edge, and the page scrolling to the footer — with a non-vacuity guard on each, because an earlier version of that sweep selected `main *` (there is no `<main>` in this app), matched nothing, and passed everywhere while proving nothing. Section 34 pins the tab separation: Yearly's names are never offered on Monthly, saving a Monthly expense never adds its category to Yearly's list, and Yearly still reports that money as a row of its own so its category rows keep adding up to its Totals | 174 |
| `test-billpay.js` | Two changes that belong together, because both are about one bill being one bill. **(1) `allot` holds the bill row's `id`, not its category name.** A month can hold two bills under one name — a $1,000 "Credit Amex" and a $500 "Credit July PC", both filed under Credit Bill — and a name made them a single pool, so each reported the pair's combined `$1,205 Itemised` against its own amount. Section 1 pins that each now reports only what was filed against it; section 2 that "Allot to" lists one option per bill row, valued by id and labelled `description — $amount`; section 3 the load migration, which repoints a name-keyed `allot` onto the **larger** bill of that month (nothing records which was meant), **detaches** one naming a bill that no longer exists rather than leaving its money in no total, and is idempotent on a second pass; section 4 that renaming the category can no longer break the link. **(2) A card payment is kept as a `Bill Payment` row instead of being dropped.** Section 5 is the scenario it was built for — July's bill $500, August's $750, August's statement carrying $1,250 of purchases and the $500 that cleared July — and pins that it reads `$1,250 Spend` under the amount with `$500 Bill Paid` under the description, that the two still account for the $750 off the PDF, that nothing is flagged as over-allotted and that July's bill is untouched. Section 6 pins that the payment appears **nowhere** else: no bar, no trend line, no Category-by-month row, not in the table's category filter, not in the month total. Section 7 pins the display merge — two payment rows draw as one `Previous Month Bill ×2` line, summed, dated the earliest, attributed to whoever the **bill** belongs to, sitting directly under it — and section 8 that the merge is display only: both rows are still in the ledger, still editable and deletable behind the toggle. Section 5b pins what replaced the bail-out bug: the reported figure used to be Spend and Bill Paid netted together and it hid itself when that net reached zero or less, so a $791 bill with $1,449 cleared and only $1,200 of charges found reported nothing at all and read as reconciled. The two facts are separate now, so neither can cancel the other — `$1,200 Spend` beside `$1,449 Bill Paid`, and the same shape whether the charges fall short of, equal, or exceed the payments. Section 9 pins `Bill Payment` last on the pickers with the other thirteen still A–Z, section 10 that Add Expense refuses a payment with no bill and stores the amount negative from a plainly-typed figure, and section 11 that every card-payment wording routes in while every transfer wording is still dropped and never mistaken for one | 53 |
| `test-yf-highlights.js` | Yearly Finance's Highlights carousel and the summary row above the cards: which slides qualify on thin vs. full years, the 60/40 top row with its reserved panel, the 8px grid rhythm shared with every other tab, card order and widths surviving the AVG/STATS swap, and the averages dividing by the current month rather than by 12 Section 8b covers the monthly chart's axis, which used to start at 5k and **double** — so the only tops on offer were 5k/10k/20k/40k and a $7,237 month was drawn against a 10k ceiling, using 72% of the height it had. It now rounds up to the next 1,000. The boundaries are pinned (7,237 → $8k, an exact 8,000 staying at $8k rather than rounding past itself, 8,001 → $9k with the half-thousand midpoint label that follows, and a tiny year still floored at $1k), and the bar's own fill is measured at ≥90% of the plot — the complaint itself, rather than a proxy for it. Every check in the section fails against the old doubling, which was verified by reverting it. | 53 |
| `test-dates.js` | Year boundaries: the contribution-room table rolling forward, pickers topping out at the right year, the last announced TFSA limit carrying into an unannounced year, and a page left open across midnight repainting itself into the new year | 26 |
| `test-ui-edits.js` | The header and badge pass: ABI/POO naming, coloured Who badges with matching filter active states, the Contributor + Description one-line pairing, and the CAD/USD + account filters staying Dashboard-only while the who-filter lives in the header | 29 |
| `test-storage.js` | The Clear-data checklist and the keep-data-here switch, plus the two bugs that prompted them. Each tick empties its own store and — the load-bearing half — leaves every other one standing, since wiping too much is the failure that cannot be undone. Yearly Finance and Monthly Expense share a single ledger and are told apart by `t.tab`, so those two ticks are row filters rather than key deletions: clearing Yearly keeps Monthly's rows and **detaches** what pointed at the bills it removed, rather than orphaning money into no total at all. The PIN, the Firebase settings and the tab order survive every combination, because wiping them would lock you out or disconnect your sync. A clear defaults to this browser only, and the `fbLocalOnly` guard is checked by driving the real 1200ms debounce and watching whether anything reaches a stubbed `fbDB` — with a companion assertion that the same call *does* write once the guard is down, so the check cannot pass vacuously. Also pins that all five persist paths advance `sparta.updatedAt` (it used to be `persist()` alone, so an evening of Yearly work never moved the stamp `fbConnect` uses and a trivial Dashboard change elsewhere outranked it), that `bootStamp` stays frozen, that the switch is locked shut with no cloud to fall back on and says why, and that with it off the only `sparta.*` keys reaching the disk are the switch itself and the PIN — the latter because the pre-boot inline script reads it directly and is the only thing preventing a flash of unlocked content. Section 8 pins the drawer-per-database scheme that closed a real data-loss incident: three nodes inside ONE Firebase project get three different ids (so `SYNC_KEY` must be in the hash, or Test/Dev/Prod would collide exactly as dummy and production once did); a trailing slash, different casing or stray spaces all resolve to the *same* drawer, since a cosmetic edit months later must not orphan one; an unconfigured app falls back to a fixed name rather than hashing an empty string; data keys are namespaced while the PIN and other device keys stay bare; and another database's rows are neither read nor disturbed — they simply sit in a drawer nothing opens. Section 5 also pins that an AUTOMATED save (the 60-second price refresh, the hourly compaction) leaves `sparta.updatedAt` and the user-edited flag alone, with a companion assertion that the identical call does both when a person makes it — a tab left open all day used to re-stamp itself every minute and then outrank a phone that had genuinely been used. Sections 2b-2c cover the compacted drawer: six ticks, one per tab in tab-bar order, sharing a single row; the notepad surviving every tick since it is a panel rather than a tab; the Archives tick being a harmless no-op until Archives has a store; the drawer capped at 75vh even as a two-column card grid; and every instruction folded away behind its own `i` button, which toggles `hidden` and `aria-expanded` together. Section 2d pins the grid itself from live rects: the running order and which cards span both columns, that a Clear data pill is exactly as tall as a Tab order pill (the drawer's blanket `input{min-height:36px}` was sizing the *checkbox*, standing them 42px against 35px), that all six tab pills and Reset to default share one line, that Cloud sync splits 60/40 over 60/20/20 with both status rows inside one container rather than two strips, and that Live prices is no longer a card but a Yes/No in the Cloud sync heading with no orphaned refresh button. It measures the gap under each paired card's last child rather than comparing the two card heights — grid items in one row always stretch to match, so the obvious check cannot fail (bug class 14). That section opens the drawer and asserts the tick boxes have real dimensions first — everything in a `display:none` drawer measures 0×0, so the row-sharing check would otherwise pass while proving nothing Sections 4b-4c close a real data-loss incident: a laptop tab left open overnight woke, its 60-second price refresh fired, and `persist()` pushed that tab's day-old ledger over five rows a phone had added — with nobody touching the laptop. The cause was that leaving `updatedAt` alone stops an automated save *winning* a merge but not *writing* one, and `fbUserEdited` is sticky for the life of a tab. 4b pins both directions (an automated save writes nothing even with the flag stuck on, **and** the same change by a person still syncs, so it cannot pass by breaking sync); 4c pins that a woken tab pulls a newer cloud copy, pushes nothing while doing so, and leaves an *older* remote alone. Verified to fail against the unfixed file. | 79 |
| `test-regression.js` | Diffs a **baseline snapshot** against the current file: rendered markup of all 5 tabs, 45 derived money values, post-boot `state`, and console errors | 15 |

`test-regression.js` needs a pre-change snapshot:

```bash
git show <ref>:"Sparta Finance Tracker/Sparta ap stock tracker.html" > tests/baseline.html
```

Without one, `run-all.sh` skips it and runs the other fourteen.

## Benchmarks

```bash
node bench.js "../Sparta ap stock tracker.html" 7   # single build
node ab.js 9                                        # interleaved A/B vs baseline.html
```

External hosts (Google Fonts, frankfurter, finnhub) are stubbed with fixed
latency so runs are deterministic. Chromium throttles rAF in headless, so
frame-gap numbers are not meaningful here — the reliable signals are load
timings, running-animation count, idle layout count, and request/error counts.

## Notes

- `FIREBASE_ENABLED` is `false` in the committed file, so no suite touches the cloud.
- Fixtures seed `localStorage` once per context; `lib.js` guards against the
  re-seed that would otherwise wipe state on reload.
- Contribution fixtures set `cad:true` + `sparta.contrib.cadFixed` so the legacy
  CAD migration does not silently multiply them by the FX rate. That migration is
  exercised on purpose in `test-data.js`.
